---
- hosts: database
  tasks:
    - name: install mysql-server
      apt: 
        name: mysql-server 
        state: present 
        update_cache: yes
      become: true

    - name: ensure mysql started
      service: 
        name: mysql 
        state: started 
        enabled: yes